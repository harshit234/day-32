# day-32
Gini impurity, entropy, information gain, overfitting &amp; pruning, Decision Tree hyperparameters, bagging &amp; bootstrap, feature randomness, Random Forest hyperparameters, feature importance, GridSearchCV, RandomizedSearchCV, cross-validation
